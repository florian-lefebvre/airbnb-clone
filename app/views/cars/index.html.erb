<h1>All cars</h1>
<% if @cars.empty? %>
  <div>No cars yet</div>
<% else %>
  <% @cars.each do |car| %>
    <div><%= @car %></div>
  <% end %>
  <%= link_to "New car", new_car_path(@car) %>
<% end %>
