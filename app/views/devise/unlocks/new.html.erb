<%= render "devise/shared/layout", title: "Resend unlock instructions", url: unlock_path(resource_name), method: :post do |f| %>
  <%= f.full_error :unlock_token %>
  <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
<% end %>
