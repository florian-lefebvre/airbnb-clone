<%= render "devise/shared/layout", title: "Forgot your password?", label: "Send instructions", url: password_path(resource_name) do |f| %>
  <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
<% end %>
